# 공부 로드맵 — Sparse-view 3DGS Regime 연구

**원칙: 모든 학습 항목은 논문의 특정 설계 결정에 연결된다.** 연결되지 않는 것은 지금 읽지 않는다.

우리가 최종적으로 주장할 인과 사슬:

```
View 조건(수·overlap)
    → Geometry uncertainty
        → Gaussian initialization 오차
            → Optimization dynamics 이상(densification·과적합)
                → Held-out rendering 품질 저하
```

각 화살표마다 필요한 이론이 다르다. 아래는 그 순서다.

---

## 완료 현황

| 항목 | 상태 |
|---|---|
| CS231A 1 (카메라 모델) | ✅ |
| CS231A 3 (에피폴라 기하) | ✅ |
| CS231A 4 (스테레오 + SfM) | ✅ (일부 재독 필요, §A 참조) |
| 3DGS 원논문 | ✅ 상당 부분 |
| 실행: DTU·COLMAP·gsplat·MVSplat 파이프라인 | ✅ 1일차 완료 |
| B. gsplat DefaultStrategy 코드 | ✅ 2일차 완료 (refine_stop_iter 보너스 발견, 신규 통제조건 도출) |
| 실행: dense-view sanity(24dB) + RE10K/DL3DV in-domain(25.6/20.0dB) + DepthSplat 통합 | ✅ 2일차 완료 |

**현재 상태 진단** — 손(구현)이 머리(이론)보다 앞서 있다. 파이프라인은 돌지만 어제 파일럿의 "Gaussian↑ PSNR↓"를 설명할 언어가 아직 없다. 아래 A·B가 그 간극을 메운다.

---

## A. 노트 4 부분 재독 (1시간, 최우선)

전체 재독 금지. 세 절만.

### A-1. §2.2 비선형 삼각측량 ★★★ (30분)

재투영 오차 최소화 → Gauss-Newton → `δP = −(JᵀJ)⁻¹Jᵀe`

**왜 지금 제일 중요한가 — 세 가지가 여기서 동시에 풀린다.**

1. **COLMAP이 실제로 하는 일** — 어제 나온 reprojection error 0.61px가 이 목적함수 값이다
2. **깊이 불확실성의 일반형** — 삼각측량 공분산 ≈ `σ²(JᵀJ)⁻¹`. baseline이 좁으면 두 시선이 거의 평행 → `JᵀJ`가 ill-conditioned → 깊이 방향 분산 폭발. 스테레오 근사식 `Z²/(fB)`보다 일반적이고 다중 view에 그대로 적용된다. **우리가 찾던 "overlap↓ → geometry uncertainty↑"의 정확한 수학적 형태**
3. **Diff3R 부록 B의 절반** — 그들이 Hessian을 `H ≈ JᵀJ`로 근사하는 것이 같은 Gauss-Newton이다

### A-2. §3.3 재구성의 모호성 ★★ (15분)

similarity ambiguity — 절대 스케일은 영상만으로 복원 불가.

→ **C2의 global scale bias 개입 `d′ = s·d`의 근거.** Omni-Scene이 저overlap FF 실패 원인으로 지목한 scale ambiguity와 같은 얘기.

### A-3. §5.1 Bundle adjustment ★ (10분, 개념만)

COLMAP이 무엇을 최적화하는지. 우리는 pose-given이라 pose는 고정 → 깊이 볼 필요 없음.

### 건너뛸 것
- §3.1~3.2 Tomasi-Kanade factorization — affine 가정, 우리와 무관
- §4 전체 (essential matrix 분해) — pose 추정용, 우리는 pose를 제공하므로 불필요

---

## B. gsplat 코드 읽기 — ✅ 완료 (2026-08-10)

값 넷 확인 완료 + 다섯 번째(보너스) 발견:

| 항목 | 값 |
|---|---|
| gradient 임계값 | `grow_grad2d = 0.0002` (2D screen-space gradient 누적 평균) |
| clone vs split | scale(정규화, scene_scale 기준) ≤0.01이면 clone, 크면 split |
| opacity reset | 3000 iteration마다, `prune_opa*2=0.01`로 리셋 |
| pruning | opacity<0.005 또는 scale이 scene_scale의 10% 초과 |
| **(보너스) densification 창** | **`refine_start_iter=500`, `refine_stop_iter=15,000`** — 15k 이후로는 densification/pruning이 완전히 멈춘다 |

**다섯 번째 값이 실험 설계를 바꾼다** — §신규 항목 참조.

<details>
<summary>원래 계획(참고용, 이미 완료)</summary>

**3DGS 논문 재독 금지.** 이미 읽었으므로 `DefaultStrategy` 구현을 직접 본다. 논문 값과 구현 값이 다를 수 있고, 우리가 보고할 것은 **우리가 돌린 구현의 동작**이다.

확인할 네 가지:

| 항목 | 왜 |
|---|---|
| view-space position gradient 임계값과 누적 방식 | densification 트리거. H1의 "Gaussian 수 증가" 메커니즘 |
| clone vs split 분기 기준 (scale 임계) | 어떤 Gaussian이 어떻게 늘어나는지 |
| opacity reset 주기와 직후 거동 | H2의 품질 궤적 해석. 정점·하강과 reset 시점이 겹치는지 |
| pruning 조건 (opacity 하한, screen-space radius 상한) | Gaussian 수 감소 구간 해석 |

**직결 문제** — 어제 파일럿의 `60s: 100k Gaussian / 10.69dB → 300s: 221k / 9.85dB`가 정말 densification 때문인지, 아니면 파이프라인 이상인지 판단하려면 이 네 개를 알아야 한다.

</details>

---

## 신규. Densification on/off 통제 조건 (B에서 파생, 2026-08-10)

`refine_stop_iter=15,000`이라는 것은 **15k 이후 구간이 자동으로 "densification 없는 최적화"**라는 뜻이다. 오늘 dense-sanity(scan1, 42-view, 30k iter)에서 iter 20,366~30,000 구간은 gaussian_count가 1,703,091로 고정된 채 PSNR만 24.11→24.06으로 미세 하강했다 — densification과 무관한 순수 optimization dynamics만으로도 하강이 있다는 뜻. 반대로 어제 sparse 8-view 붕괴(100k→221k, 10.69→9.85dB)는 iter<1749로 densification이 활발한 구간에서 일어났다. **두 하강이 같은 메커니즘이 아닐 수 있다.**

- **H1 검증에 필요**: H1은 "densification 후 Gaussian 수는 늘지만 품질은 떨어진다"인데, densification 없이 같은 시간을 쓴 조건과 비교해야 이게 densification 때문인지 순수 optimization 문제인지 분리된다.
- **C1-b on/off 결정에 직결**: ForeSplat이 post-optimization에서 densification을 끄는 이유와 같은 근거. → **C1-b는 on/off 두 조건 모두 돌리는 것으로 결정**(비용 2배, but H1 핵심 증거 + 선행 연구와 비교 가능성 확보).
- **실행**: `refine_stop_iter`를 낮추거나(예: 0) `DefaultStrategy`를 densification 없는 조건으로 하나 더 돌려서 같은 budget 지점의 궤적을 비교.

---

## C. Sparse-view geometry 마무리 (반나절)

A-1에서 `JᵀJ` 조건수를 이해했다면 나머지는 짧다.

### C-1. baseline vs overlap 트레이드오프 ★★★

강의노트에 명시적으로 없는 부분 — **직접 정리해야 한다.**

- baseline ↑ → 삼각측량 정밀도 ↑ (`JᵀJ` 조건수 개선)
- baseline ↑ → 시점 차이 ↑ → 매칭 난이도 ↑, overlap ↓

**두 힘이 반대 방향.** 그래서 최적 baseline이 중간에 존재하고, **"baseline이 크다 = overlap이 낮다"가 아니다.**

→ **§5.3의 정당화 문단.** 우리가 overlap을 baseline으로 통제하지 않고 co-visibility 수치로 직접 정의한 이유.

자료: Szeliski 2판 12장(무료 PDF), 보조로 Gallup et al., "Variable Baseline/Resolution Stereo" (CVPR 2008)

### C-2. 매칭 실패의 구조 ★★

textureless·반복 패턴·가려짐에서 correspondence가 실패하는 조건.

→ **§5.3에서 매칭 실패 쌍을 제외하지 않고 `O_ij = 0`으로 포함하기로 한 결정의 근거.** 실패 자체가 저overlap의 증거이므로, 빼버리면 저텍스처 장면에서 overlap이 체계적으로 과대평가된다.

자료: CS231A 노트 6 (Fitting and Matching), Szeliski 12장 correspondence 파트

---

## D. 실측 산출물 — 공부가 곧 figure (반나절)

**A-1 직후에 할 것.** 이미 있는 COLMAP 출력을 재활용하므로 새 실험이 아니다.

1. scan1의 view 쌍별 baseline 계산
2. COLMAP triangulation 점들의 깊이·재투영 오차 추출
3. `(JᵀJ)⁻¹`의 깊이 방향 성분(또는 근사로 `Z²/(fB)`) vs baseline·overlap 곡선

**얻는 것 세 가지**
- 이론 이해의 검증 (이론값과 실측이 맞는지)
- **§5.3 overlap 정의를 정당화하는 논문 figure**
- H1의 "초기 geometry 오차"가 어떤 조건에서 커지는지 실데이터 근거

**추가 (2026-08-10)**: 학습 궤적 figure에 두 가지를 겹친다.
4. iteration 축에 `refine_stop_iter=15,000` 지점을 수직선으로 표시
5. Gaussian count 곡선을 PSNR 곡선과 같은 그림에 겹쳐 그리기 — "count는 고정인데 품질만 하강"하는 구간(신규 항목의 densification on/off 근거)이 시각적으로 바로 드러난다

데이터 위치: `experiments/outputs_dense_sanity/colmap_work/dtu_scan1_dense_sanity/49view_seed0/sparse_triangulated/`에 `points3D.bin`/`images.bin` 이미 있음 (오늘 42-view dense-sanity 실행 때 생성됨, pycolmap으로 바로 읽을 수 있음) — 새로 실험 안 돌려도 됨.

### D 1차 실측 결과 — 예상과 반대로 나옴, A-1 재독 후 재논의 필요 (2026-08-10)

`experiments/scripts/geometry_uncertainty_figure.py`로 1~3 먼저 계산해봄 (audit log §13에 전체 기록).

- baseline↑ → overlap↓ (corr −0.87), baseline↑ → JᵀJ 기반 depth 불확실성↓ (corr −0.95) — **여기까진 이론대로.**
- 근데 **overlap과 depth 불확실성을 직접 비교하면 +0.95로 양의 상관** — overlap 높을수록 불확실성도 높게 나옴. baseline이 둘 다를 같은 방향으로 끌어내리는 confounder라서 그럼(버그 아님).
- **A-1 읽으면서 같이 풀 질문 두 개**: ① baseline을 world-unit 거리로 썼는데, 조건수에 진짜 중요한 게 raw 거리가 아니라 물체 기준 시선 각도 아닌가? ② 이 계산은 point마다 2-view만 가정했는데(실제 track length 평균 6.05), pairwise `O_ij`가 재는 것과 "이 두 view만 있었다면"의 불확실성이 같은 걸 재고 있는 게 맞나?

**§5.3에 "overlap이 uncertainty를 낮춘다"고 단순하게 쓰면 안 될 것 같다** — A-1 끝나고 이 결과부터 다시 보기.

---

## E. 다음 단계 — MVS와 cost volume (황인재 주도)

### E-1. CS231A 노트 5 (Active and Volumetric Stereo) ★★★

**plane sweep이 여기 있다.** 깊이 가설을 여러 개 세우고 각 가설로 워핑해 일치도를 재는 구조 — MVSNet cost volume의 직계 조상이며 MVSplat이 하는 일이 정확히 이것.

**MVSNet 읽기 전에 반드시 이것부터.**

### E-2. MVSNet (Yao et al., ECCV 2018)

cost volume의 원형. plane sweep + 분산 기반 cost volume → depth 회귀.

**C4-a와 직결** — 읽으면서 반드시 메모할 것: *matching cost 분포에서 신뢰도를 어떻게 뽑을 수 있는가?* 최소 cost와 차선 cost의 비, depth 확률 분포의 엔트로피·분산 등. MVSplat에 명시적 confidence가 없을 경우 이것이 대체 신호가 된다.

### E-3. MVSplat → DepthSplat

앞의 둘을 마쳤으면 빠르게 읽힌다. 뽑을 것은 팀 가이드의 논문별 항목 참조.

---

## F. 후순위 (필요할 때)

| 항목 | 언제 |
|---|---|
| CS231A 노트 10 (Optimal Estimation) | A-1에서 `JᵀJ`를 이해했으면 급하지 않음. C2 서술 쓸 때 |
| CS231A 노트 8 (Monocular Depth, scale) | DepthSplat 읽을 때 함께 |
| MAML / Implicit Gradients | Diff3R·ForeSplat 관계 서술 쓸 때. "왜 unrolling이 비싸고 implicit gradient가 대안인가" 수준이면 충분 |
| RegNeRF / FreeNeRF | H1~H3 서술 보강용. sparse-view 과적합의 선행 진단 수집 |
| 통계 (cluster bootstrap) | 서창희 담당, 파일럿 전까지. Cameron & Miller 실무 가이드 |

**건너뛸 것**: CS231A 노트 2(단일 시점 계측), 7(표현 학습), 9(optical flow) — 우리 사슬에 연결되지 않음.

---

## 일정 요약

| 순서 | 내용 | 시간 | 담당 |
|---|---|---|---|
| 1 | A. 노트 4 §2.2·§3.3·§5.1 재독 | 1시간 | 이용수 | **미완 — 지금 병목, 최우선** |
| 2 | ~~B. gsplat DefaultStrategy 코드~~ | 30분 | 이용수 | ✅ 완료 (2026-08-10) |
| 3 | D. 실측 곡선 (§5.3 figure, 15k 표시+gaussian count 겹치기) | 반나절 | 이용수 | A-1 이후 진행 가능 |
| 4 | C. baseline vs overlap 정리 | 반나절 | 이용수·서창희 | DepthSplat context 결과로 동기 강화됨 |
| 5 | E-1. 노트 5 plane sweep | 2시간 | 황인재 | |
| 6 | E-2. MVSNet | 반나절 | 황인재 | |
| 7 | E-3. MVSplat·DepthSplat | 1일 | 황인재 | |
| 신규 | Densification on/off 통제 조건 설계 (C1-b on/off 결정) | - | 이용수 | B에서 파생, 결정됨: on/off 둘 다 |
| 병행 | RE10K 확보(본 실험용, probe 말고 본 규모) | - | (팀) | 이론 공부와 별도 트랙으로 계속 굴러가야 함 — 안 그러면 4~6주차 벤치마크 시작 못 함 |

**A-1이 지금 가장 수익률 높은 한 시간이다.** D가 §5.3 정당화 figure로 논문에 직접 들어가는데 A-1 없이는 D를 못 쓴다.

---

## 학습 항목 → 설계 결정 매핑 (한 장 요약)

| 학습 항목 | 이것이 정당화하는 것 |
|---|---|
| §2.2 Gauss-Newton, `JᵀJ` 조건수 | H1 이론 근거, 깊이 불확실성의 정의 |
| §3.3 similarity ambiguity | C2의 scale bias 개입 `d′ = s·d` |
| gsplat densification 구현 | H1(Gaussian 수↑ 품질↓), H2(정점 후 하강) 해석 |
| `refine_stop_iter` (densification 창) | C1-b on/off 결정 근거, H1을 densification-only 효과로 분리 |
| DepthSplat context 간격 vs PSNR(20↔12dB) | V2(지배 축) 초기 신호, context 선정을 co-visibility 기반으로 바꿔야 하는 이유 |
| baseline vs overlap 트레이드오프 | §5.3 co-visibility 기반 overlap 정의 |
| 매칭 실패 구조 | §5.3 non-edge = 0 포함 결정 |
| plane sweep / cost volume | §5.2 모델 이해, C4-a 대체 confidence 신호 |
| cluster bootstrap | §5.12 통계, τ 산출 |

---

## 지켜야 할 것

1. **연결되지 않는 것은 읽지 않는다** — 위 매핑 표에 없으면 지금은 후순위
2. **논문보다 코드** — 이미 읽은 것(3DGS)은 구현을 본다
3. **읽으면서 산출물을 만든다** — D처럼 학습이 figure나 설계 근거로 남게 한다
4. **막히면 실측으로 확인** — 이론이 이해 안 되면 DTU 데이터로 수치를 찍어본다